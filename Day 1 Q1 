Write a program to input two numbers and display their sum.

#include <stdio.h>

int a,b ;
int main ( ) {
printf("enter a:");
scanf("%d", & a);

printf("enter b:");
scanf("%d", & b);

printf("sum is: %d", a+b);

return 0;
}
